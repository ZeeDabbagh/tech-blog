<div class="card">
  <h2>{{blog.title}}</h2>
  <h6>{{blog.user.name}}
    <span><small>{{formatDate blog.blog_date}}</small></span></h6>
  <p>{{blog.blog_text}}</p>
  <div class="card">
    {{#if blog.comments.length}}
      <h6>Comments </h6>
      {{#each blog.comments as |comment|}}
        <div>
          <p>{{comment.user.name}}
            <span><small>{{formatDate comment.comment_date}}</small></span></p>
          <p>{{comment.comment_text}}</p>
        </div>
      {{/each}}
    {{/if}}
    {{#if logged_in}}
      <form class="comment-form">
        <label><strong>Leave Comment Here:</strong></label>
        <textarea type="text" id="comment-text" class="input"></textarea>
        <button type="submit" class="btn">Submit Comment</button>
      </form>
      <script src="/js/createComment.js"></script>
    {{/if}}
  </div>
  {{#if canEdit}}
    <button id="edit-btn">Edit</button>
    <button id="delete-btn">Delete</button>
    <form class="blog-form" style="display: none">
      <label>Title:

        <input type="text" id="blog-title" value="{{blog_title}}" />
      </label>
      <label>
        Content:
        <textarea
          type="text"
          id="blog-text"
          defaultValue="{{blog_text}}"
        >{{blog_text}}</textarea>
      </label>
      <input type="submit" />
    </form>

    <script src="/js/editBlog.js"></script>
  {{/if}}
</div>